import Table from '@/components/Table';
import links from '../../data/community/community.json';

export default function Home() {
	return (
		<>
			<p>needs update - incorrect info</p>
			<Table programs={links} />
		</>
	);
}
